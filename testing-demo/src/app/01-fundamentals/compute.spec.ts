import {compute} from '.compute.ts';

describe('compute', () => {
it('it will return zero', () => {
let reuslt = compute(0);
expect(result).toBe(0);
})

it('it will return one', () => {
let result = compute(1);
expect(result).toBe(2);
})
})